import 'package:flutter/material.dart';

class RoupasList extends StatelessWidget {
  const RoupasList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
